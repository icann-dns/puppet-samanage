fixtures:
    symlinks:
      samanage: "#{source_dir}"
    repositories:
      stdlib:
        repo: https://github.com/puppetlabs/puppetlabs-stdlib.git
        ref: 4.12.0
      tea:
        repo: https://github.com/icann-dns/puppet-tea
        ref: 0.2.5
